# Server Configuration
server.port=8080
spring.application.name=employee-loan-management

# Database Configuration (H2 In-Memory)
spring.datasource.url=jdbc:h2:mem:employeeloandb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (optional, for debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JWT Configuration
jwt.secret=unilak-employee-loan-management-system-secret-key-2025-msit6120
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:5173

# Logging
logging.level.com.unilak.employeeloan=DEBUG
